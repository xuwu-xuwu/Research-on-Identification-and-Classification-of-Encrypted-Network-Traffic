# Source Layout

The current source tree keeps only the encryption-method identification pipeline.

- `encryption_method/`: unified dataset construction, full 9-class training, paper-method comparisons, and enhanced current-method training.

Archived application/business traffic classification code is under:

- `legacy_business_traffic_classification/src/`
